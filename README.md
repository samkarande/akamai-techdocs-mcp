# akamai-techdocs-mcp
Maintains a list of tech doc urls. Updates the url list for users on periodic frequency. Fetches the latest docs for every user periodically without any user intervention. Simple to install with a click or command. 
